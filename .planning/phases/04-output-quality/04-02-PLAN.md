---
phase: 04-output-quality
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/phase4/snapshot.test.js
  - tests/phase4/__snapshots__/snapshot.test.js.snap
autonomous: true
requirements: [QUALITY-03]

must_haves:
  truths:
    - "JS fixture snapshot matches committed baseline"
    - "Python fixture snapshot matches committed baseline"
    - "Re-running snapshot tests without code changes produces no diff"
  artifacts:
    - path: "tests/phase4/__snapshots__/snapshot.test.js.snap"
      provides: "Committed snapshot baselines for JS and Python fixtures"
  key_links:
    - from: "tests/phase4/snapshot.test.js"
      to: "src/generators/mdGenerator.js"
      via: "generateInstructions call with testDate"
      pattern: "generateInstructions.*testDate"
    - from: "tests/phase4/snapshot.test.js"
      to: "tests/fixtures/js-project/utils/helpers.js"
      via: "fixture source read + analysis"
      pattern: "js-project"
    - from: "tests/phase4/snapshot.test.js"
      to: "tests/fixtures/py-project/utils/helpers.py"
      via: "fixture source read + analysis"
      pattern: "py-project"
---

<objective>
Generate and commit initial snapshot baselines by running the snapshot tests with --updateSnapshot, then verify stability.

Purpose: Establish the regression baseline (QUALITY-03) so any future change to mdGenerator output is caught by snapshot diff.
Output: Committed .snap file with baselines for both JS and Python fixtures. Snapshot tests GREEN.
</objective>

<execution_context>
@/Users/carlosmiret/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carlosmiret/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-output-quality/04-CONTEXT.md
@.planning/phases/04-output-quality/04-RESEARCH.md
@.planning/phases/04-output-quality/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (now implemented) -->

From src/generators/mdGenerator.js:
```javascript
export function generateInstructions(folderInfo, patternInfo, languageInfo, projectMeta) { ... }
export { buildConventionsSection, buildUsageExamplesSection, buildDontDoSection, buildMarkdown }
```

<!-- projectMeta now accepts testDate -->
// projectMeta.testDate overrides new Date() for deterministic timestamps
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate snapshot baselines and verify stability</name>
  <files>tests/phase4/snapshot.test.js, tests/phase4/__snapshots__/snapshot.test.js.snap</files>
  <action>
**Step 1: Review and fix snapshot test if needed**

Read `tests/phase4/snapshot.test.js` (created in W0). Verify it:
- Copies fixtures to tmpDir before running (so generated files don't pollute committed fixtures)
- Passes `testDate: '2026-01-01'` in projectMeta for stable timestamps
- Uses `detectFolderPattern` + `generateInstructions` pipeline
- Reads the generated file content and calls `expect(content).toMatchSnapshot()`
- Cleans up tmpDir in afterAll

If the test has any issues (e.g., missing imports, wrong fixture paths, incorrect folderInfo shape), fix them now. Common issues:
- `folderInfo.path` must point to the tmpDir copy, not the original fixture
- `codeFiles` must be populated from `readdirSync` filtering by extension
- For py-project, languageInfo should be `{ lang: 'Python', framework: 'None' }`

**Step 2: Generate initial snapshots**

Run: `node --experimental-vm-modules node_modules/.bin/jest --testPathPatterns "phase4/snapshot" --updateSnapshot`

This creates `tests/phase4/__snapshots__/snapshot.test.js.snap` with the baseline content.

**Step 3: Verify stability (idempotency)**

Run the same tests again WITHOUT --updateSnapshot:
`node --experimental-vm-modules node_modules/.bin/jest --testPathPatterns "phase4/snapshot"`

Both tests must pass — proving the output is deterministic when testDate is fixed.

**Step 4: Verify snapshot content is reasonable**

Read the generated .snap file. Verify:
- It contains two snapshot entries (js-project and py-project)
- Each snapshot starts with `<!-- generated: 2026-01-01 -->`
- JS snapshot contains `## Project Conventions` with camelCase detection (if fixture has 5+ functions)
- Python snapshot contains analysis of the Python fixture
- Neither snapshot contains `## Don't Do` section (fixtures designed to avoid antipatterns)
- Neither snapshot exceeds 300 lines

If snapshot content looks wrong (e.g., empty sections, unexpected antipatterns), fix the fixture files in `tests/fixtures/` and regenerate.
  </action>
  <verify>
    <automated>cd /Users/carlosmiret/Desktop/skill-me-up && node --experimental-vm-modules node_modules/.bin/jest --testPathPatterns "phase4/snapshot" -x</automated>
  </verify>
  <done>Snapshot file exists at tests/phase4/__snapshots__/snapshot.test.js.snap with baselines for both fixtures. Running tests twice in a row produces identical results (idempotent). Full suite green.</done>
</task>

</tasks>

<verification>
Full test suite passes including snapshots:
```bash
node --experimental-vm-modules node_modules/.bin/jest
```

Snapshot stability check (run twice, both pass):
```bash
node --experimental-vm-modules node_modules/.bin/jest --testPathPatterns "phase4/snapshot" && node --experimental-vm-modules node_modules/.bin/jest --testPathPatterns "phase4/snapshot"
```
</verification>

<success_criteria>
- tests/phase4/__snapshots__/snapshot.test.js.snap exists and is committed
- Snapshot contains baselines for both js-project and py-project
- Both snapshot tests pass GREEN
- Running tests twice produces identical results (idempotent)
- Full suite (all phases) GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-quality/04-02-SUMMARY.md`
</output>
