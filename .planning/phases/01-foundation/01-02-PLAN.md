---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/analyzer/patternDetector.js
autonomous: true
requirements:
  - ENRICH-01

must_haves:
  truths:
    - "Every method object returned by analyze* functions carries a lineNumber integer field"
    - "Every class declaration extracted by analyze* carries a lineNumber integer field"
    - "lineNumber is 1-based (line 1 = first line of file)"
    - "Line numbers are derived from match.index on the original content string — not from a counter variable"
  artifacts:
    - path: "src/analyzer/patternDetector.js"
      provides: "getLineNumber private helper + lineNumber on all method/class objects"
      contains: "function getLineNumber"
  key_links:
    - from: "getLineNumber"
      to: "content.substring(0, matchIndex).split('\\n').length"
      via: "direct computation — no state"
      pattern: "getLineNumber"
    - from: "each analyze* function"
      to: "method push objects"
      via: "lineNumber: getLineNumber(content, match.index)"
      pattern: "lineNumber:"
---

<objective>
Add source line number tracking to all `analyze*` functions in `patternDetector.js`. Each method and class declaration object gains a `lineNumber: integer` field (1-based). This is a prerequisite for Phase 2 snippet extraction and file+line pointers.

Purpose: Phase 2 needs per-method line pointers to extract code snippets. Without `lineNumber` on each method object, snippet extraction cannot know where to read.
Output: Modified `patternDetector.js` where all 8 language analyzers attach `lineNumber` to every method and class object they produce.
</objective>

<execution_context>
@/Users/carlosmiret/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carlosmiret/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/analyzer/patternDetector.js

<interfaces>
<!-- Exact current method object shapes. Executor adds lineNumber to each. -->

Current method object shape (same across all analyze* functions):
```javascript
{
  name: string,
  params: string,
  returnType: string,
  annotation: string | null,
  // NEW: lineNumber: integer  <-- add this to every push
}
```

Current class detection patterns (use match.index for each):

analyzeJava — class detection at line ~120:
  content.match(/.../); // returns match with .index available IF changed to regex.exec()
  // But classType/className from typeMatch don't need lineNumber (only methods do per research)
  // METHOD lineNumber: extractPublicMethods() at line ~850 — uses methodRegex.exec() loop

analyzeKotlin — methods in extractKotlinMethods() at line ~193:
  while ((match = regex.exec(content)) !== null) { ... }  // match.index IS available

analyzeTypeScriptOrJs — functions at line ~237:
  while ((fn = fnRegex.exec(content)) !== null) { ... }   // fn.index IS available
  // functions array stores only strings currently: functions.push(fn[1] || fn[2])
  // MUST be changed to store objects with name + lineNumber

analyzePython — methods at line ~302:
  while ((m = methodRegex.exec(content)) !== null) { ... }  // m.index IS available

analyzeGo — methods at line ~349:
  while ((fn = fnRegex.exec(content)) !== null) { ... }   // fn.index IS available

analyzeCSharp — methods at line ~395:
  while ((m = methodRegex.exec(content)) !== null) { ... } // m.index IS available

analyzePhp — methods at line ~435:
  while ((m = methodRegex.exec(content)) !== null) { ... } // m.index IS available

analyzeRuby — methods at line ~473:
  while ((m = methodRegex.exec(content)) !== null) { ... } // m.index IS available

extractPublicMethods (Java) — at line ~850:
  while ((match = methodRegex.exec(content)) !== null) { ... } // match.index IS available

CRITICAL: analyzeTypeScriptOrJs currently stores functions as bare strings:
  functions.push(fn[1] || fn[2])
  then: methods: functions.map(f => ({ name: f, params: '', returnType: '', annotation: null }))
  → MUST change to push objects directly in the while-loop to capture fn.index

CRITICAL: analyzeTypeScriptOrJs class detection uses content.match() which does NOT provide .index
  → For now, only add lineNumber to function/method entries (not to the top-level className)
  → Class-level lineNumber can be skipped (it's not a method entry)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add getLineNumber helper and wire lineNumber into all analyze* functions</name>
  <files>src/analyzer/patternDetector.js, tests/phase1/lineTracking.test.js</files>
  <behavior>
    - analyzeJava (via extractPublicMethods): method at line 5 in a 10-line content string returns lineNumber: 5
    - analyzeTypeScriptOrJs: exported function `export function foo()` on line 3 returns lineNumber: 3
    - analyzePython: `def myMethod(self):` on line 4 returns lineNumber: 4
    - analyzeKotlin: `fun doSomething()` on line 2 returns lineNumber: 2
    - analyzeGo: exported func `func DoWork()` on line 3 returns lineNumber: 3
    - lineNumber is 1-based (first line = 1)
    - Single-line content with a method at position 0 returns lineNumber: 1
  </behavior>
  <action>
**Step 1 — Red: Update tests/phase1/lineTracking.test.js**

Replace the .todo() stubs with real tests that import detectFolderPattern and use synthetic fixture content. Since analyze* functions are private, test via detectFolderPattern with a real folder containing synthetic content files — OR, export getLineNumber and specific analyze* functions at the bottom of patternDetector.js for testability, then import them directly.

Recommended approach: Export `getLineNumber` and the 5 priority analyze* functions as named exports at the bottom of patternDetector.js (after writing tests). Test directly.

Write tests that:
1. Construct a minimal synthetic content string with a known method at a known line
2. Call the analyze* function directly (after export is added)
3. Assert result.methods[0].lineNumber equals the expected line number

**Step 2 — Implement in patternDetector.js**

Add `getLineNumber` private helper after `extractFirst` (near line 880):
```javascript
/**
 * Computes 1-based line number of a match from its index in the content string.
 * @param {string} content - Full file content
 * @param {number} matchIndex - match.index from regex exec
 * @returns {number} 1-based line number
 */
function getLineNumber(content, matchIndex) {
  return content.substring(0, matchIndex).split('\n').length
}
```

Then update each function:

**analyzeJava** — in `extractPublicMethods(content, 'java')`:
Add `lineNumber: getLineNumber(content, match.index)` to the methods.push object.
Pass `content` into `extractPublicMethods` (already receives it via closure — check signature).
If extractPublicMethods signature is `(content, language)`, it already has content. Just add the field.

**analyzeKotlin** — in `extractKotlinMethods(content)`:
Add `lineNumber: getLineNumber(content, match.index)` to the methods.push object.

**analyzeTypeScriptOrJs** — CRITICAL CHANGE:
The current code builds a `functions` array of strings then maps them. Change to collect objects directly in the while-loop:
```javascript
const functions = []
const fnRegex = /(?:export\s+(?:async\s+)?function\s+(\w+)|(?:export\s+)?const\s+(\w+)\s*=\s*(?:async\s+)?\()/g
let fn
while ((fn = fnRegex.exec(content)) !== null) {
  functions.push({
    name: fn[1] || fn[2],
    params: '',
    returnType: '',
    annotation: null,
    lineNumber: getLineNumber(content, fn.index),
  })
}
```
Then change the return object's `methods` field from `functions.map(...)` to just `functions`.

**analyzePython** — in the methodRegex while-loop:
Add `lineNumber: getLineNumber(content, m.index)` to the methods.push object.

**analyzeGo** — in the fnRegex while-loop:
Add `lineNumber: getLineNumber(content, fn.index)` to the methods.push object.

**analyzeCSharp** — in the methodRegex while-loop:
Add `lineNumber: getLineNumber(content, m.index)` to the methods.push object.

**analyzePhp** — in the methodRegex while-loop:
Add `lineNumber: getLineNumber(content, m.index)` to the methods.push object.

**analyzeRuby** — in the methodRegex while-loop:
Add `lineNumber: getLineNumber(content, m.index)` to the methods.push object.

**Export for testing** — at the bottom of patternDetector.js, add test exports:
```javascript
// ─── Test Exports (used only by tests/phase1/) ───────────────────────────────
export { getLineNumber, analyzeJava, analyzeKotlin, analyzeTypeScriptOrJs, analyzePython, analyzeGo }
```

These are additive exports — existing consumers only import `detectFolderPattern`, so this is non-breaking.

**Pitfall guards:**
- Do NOT use a counter variable. Always use `getLineNumber(content, match.index)`.
- Do NOT reset match objects — `exec()` on a `g`-flag regex retains `lastIndex` correctly in a while-loop.
- The `analyzeTypeScriptOrJs` refactor is the highest-risk change. Verify methods shape is still `[{ name, params, returnType, annotation, lineNumber }]` after the change.
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/.bin/jest --testPathPattern=phase1/lineTracking 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - tests/phase1/lineTracking.test.js: all tests green (no .todo)
    - Each analyze* function produces method objects with `lineNumber: integer` field
    - `getLineNumber` exported from patternDetector.js
    - Running `npx skill-me-up` on the project itself produces no errors (existing behavior preserved)
  </done>
</task>

</tasks>

<verification>
Full phase1 suite still passes:
  `node --experimental-vm-modules node_modules/.bin/jest --testPathPattern=phase1`

Smoke test (existing output behavior unchanged):
  `node bin/cli.js . 2>&amp;1 | head -20` — no errors, generates .md files as before

Spot-check lineNumber presence in output of detectFolderPattern by adding a temporary console.log or by reading the test assertions.
</verification>

<success_criteria>
- `getLineNumber` function exists in patternDetector.js
- All 8 analyze* functions attach `lineNumber: integer` to every method push
- lineTracking.test.js all green
- Full `node --experimental-vm-modules node_modules/.bin/jest` still passes (conventions/mdGenerator/integration stubs still .todo)
- Existing `npx skill-me-up` output is not broken (additive change only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
